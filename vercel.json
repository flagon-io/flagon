{
    "$schema": "https://openapi.vercel.sh/vercel.json",
    "framework": "nextjs",
    "buildCommand": "npm run db:provision && npm run db:migrate && npm run build",
    "git": {
        "deploymentEnabled": {
            "main": true
        }
    }
}
